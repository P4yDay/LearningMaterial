<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <style>
        body{background: skyblue; font-family: Verdana; color: #fff; padding: 30px;}
        h1{font-size: 40px; text-transform:uppercase; letter-spacing: 2px; text-align: center; }
        h2{font-size: 30px; text-transform:uppercase; letter-spacing: 2px; text-align: center; }
        p,li{font-size: 16px; text-align: center;}
    </style>

    <title>Document</title>
</head>
<body>
    <!-- Adding a partial view nav bar -->
    <% include('partials/nav.ejs') %>

    <!-- adding:  <%= person %>  known as a ejs dataType
         so that it can dynamically add the name of the value in 
        templateEngine.js -->
    <h1> Welcome to the profile of <%= person %> </h1>
    <p>  Age: <strong> <%= data.age %> </strong></p>
    <p>  Position: <strong> <%= data.job %> </strong></p>
    
    <h2> Hobbies </h2>
    <ul>
        <% data.hobbies.forEach(function(item){ %>
        <li> <%= item %> </li>
        <% }); %>
    </ul>
</body>
</html>